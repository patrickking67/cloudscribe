# Project-scoped Codex config for cloudscribe.
# Loaded only when this project is trusted in ~/.codex/config.toml.
# See https://developers.openai.com/codex/config-basic for available keys.

# No project-specific overrides at this time; inherit user defaults.
